# Joey's AI Agent - 開發計畫

> **願景**：素材進去，網站出來
> LINE 輸入指令 → 自動建站 → 部署上線 → 回傳網址

---

## 核心功能

### 目前（Phase 1）
```
LINE 訊息 → Mac mini 服務 → 自動建立網站 → 部署到 Render → 回傳公開網址
```

### 未來擴展
- 多種輸入方式（LINE、網頁前端、API）
- 提供同事使用的網頁介面
- 自訂域名 + Cloudflare DNS 自動綁定

---

## 目前狀態（2026-02-03）

### 已完成 ✅

| 項目 | 狀態 | 說明 |
|------|------|------|
| LINE Webhook | ✅ | 接收訊息、回覆確認 |
| Notion 整合 | ✅ | 建立任務、追蹤進度、儲存結果 |
| Claude API 分析 | ✅ | 任務解析、產生結構化 Prompt |
| Claude Code 執行 | ✅ | Headless 模式自動執行 |
| Google Drive MCP | ✅ | 下載客戶素材 |
| GitHub 推送 | ✅ | 使用 gh CLI 建立 Repo |
| 網站建立 | ✅ | HTML/CSS/JS 自動生成 |
| Skills 系統 | ✅ | 可重複使用的工作流程模板 |
| Render 整合 | ✅ | 程式碼已更新，待測試 |

### 待測試 🧪

- [ ] Render 部署完整流程（太空貓咖啡館 V3）

---

## Phase 1：LINE → 網站（進行中）

### 目標
從 LINE 訊息到公開網站，全自動化

### 流程
```
1. Joey 發 LINE 訊息：「幫我建立 XXX 網站」
2. 系統自動：
   - 下載素材（從 Google Drive）
   - 建立網站（HTML/CSS/JS）
   - 推送到 GitHub
   - 部署到 Render
3. 回傳 LINE：網站網址 + Notion 記錄連結
```

### 待完成
- [ ] 測試 Render 部署流程
- [ ] 確認 LINE 通知穩定發送
- [ ] 建立自動同步機制（本機 → Mac mini）

---

## Phase 2：網域整合

### 目標
一鍵完成：網站部署 + 自訂域名

### 功能
- Cloudflare DNS 自動設定
- SSL 證書自動配置
- 可選：自動購買域名

### 流程
```
LINE: 「建立官網，網域用 example.com」
↓
自動完成所有設定
↓
回傳: https://example.com
```

---

## Phase 3：多輸入方式

### 目標
讓同事也能使用

### 計畫中的輸入方式

| 方式 | 使用者 | 優先級 |
|------|--------|--------|
| LINE | Joey | ✅ 已完成 |
| 網頁前端 | 同事 | 規劃中 |
| API | 程式整合 | 規劃中 |

### 網頁前端構想
```
簡單的表單頁面：
- 輸入專案名稱
- 上傳素材（或貼 Google Drive 連結）
- 選擇網站類型
- 點擊「開始建立」
- 等待完成，顯示網址
```

---

## 技術架構

```
┌─────────────────────────────────────────────────────────────┐
│                     輸入方式                                 │
│         LINE │ 網頁前端（未來）│ API（未來）                  │
└─────────────────────────────┬───────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│               Mac mini Python 服務 (FastAPI)                 │
├─────────────────────────────────────────────────────────────┤
│  Stage 1: Claude API 分析（秒級）                            │
│  Stage 2: Claude Code 執行（分鐘級）                         │
│           ├─ Google Drive MCP → 下載素材                    │
│           ├─ GitHub CLI → 建立 Repo                         │
│           └─ Render API → 部署上線                          │
└─────────────────────────────┬───────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  輸出：通知（LINE/網頁）+ Notion 記錄 + 公開網址              │
└─────────────────────────────────────────────────────────────┘
```

---

## 測試記錄

### 太空貓咖啡館 V1
- ✅ 網站建立成功
- ❌ 部署到 GitHub Pages（應該用 Railway）

### 太空貓咖啡館 V2
- ❌ Railway Token 認證失敗（Railway Bug）

### 太空貓咖啡館 V3（待測試）
- 目標：驗證 Render 部署
- 預期：成功部署到 .onrender.com

---

## 自動化原則

> **Joey 的要求**：追求最高自動化，不要叫 Joey 做任何可以自動化的事

### 已自動化
- ✅ 任務接收（LINE Webhook）
- ✅ 任務分析（Claude API）
- ✅ 任務執行（Claude Code）
- ✅ 結果通知（LINE Push）

### 待自動化
- [x] 程式碼同步（本機 → Mac mini）✅ 已設定
- [x] 服務重啟 ✅ 已設定
- [ ] 錯誤自動修復

---

## Mac mini 連線資訊

> **重要**：Claude 必須記住這些資訊，自動執行同步，不要問 Joey

### SSH 連線
```bash
# 使用 ~/.ssh/config 中的別名
ssh macmini-remote

# 實際設定
Host: macmini-remote
IP: 100.116.178.96 (Tailscale)
User: joeyserver
Key: ~/.ssh/id_ed25519
```

### 同步指令
```bash
# 同步程式碼
rsync -avz --exclude '.git' --exclude '__pycache__' --exclude '.env' --exclude 'tasks/' \
  "/Users/JoeyLiao/Joey's AI Agent /joey-ai-agent/" macmini-remote:~/joey-ai-agent/

# 重啟服務
ssh macmini-remote "pkill -f 'uvicorn.*joey-ai-agent' && sleep 2 && cd ~/joey-ai-agent && nohup /usr/bin/python3 -m uvicorn src.main:app --host 0.0.0.0 --port 8001 > /tmp/agent.log 2>&1 &"

# 檢查健康狀態
ssh macmini-remote "curl -s http://localhost:8001/health"
```

### 服務資訊
- Port: **8001**（不是 8000）
- 路徑: `/Users/joeyserver/joey-ai-agent/`
- Log: `/tmp/agent.log`

---

## 參考資源

- [Render API](https://api-docs.render.com/)
- [Cloudflare API](https://developers.cloudflare.com/api/)
- [Skills 模板](./joey-ai-agent/skills/)

---

*最後更新：2026-02-03 01:45*
