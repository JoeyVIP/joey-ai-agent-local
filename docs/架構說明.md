# Joey AI Agent - 系統架構與 GitHub Repo 對照表

> 建立日期：2026-02-03
> 目的：釐清所有 GitHub repo 和部署架構，避免日後混淆

---

## GitHub Repos 總覽（核心 3 個）

| Repo 名稱 | 用途 | 部署位置 | 狀態 |
|-----------|------|----------|------|
| `joey-ai-agent` | Mac mini 後端（主要）| Mac mini via Cloudflare | ✅ 使用中 |
| `joey-ai-agent-local` | 本地開發備份 | 無部署 | 📦 備份用 |
| `joey-ai-frontend` | Web 前端 (Next.js) | Render Static Site | ✅ 使用中 |

---

## 系統架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                        使用者入口                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  LINE Bot                         Web Frontend                   │
│  (訊息 → LINE Platform)           https://joey-ai-frontend       │
│       │                              .onrender.com               │
│       │                                   │                      │
│       │                                   │ HTTPS                │
│       ▼                                   ▼                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                   Cloudflare Tunnel                         │ │
│  │                 ai-agent.89115053.xyz                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
│                              ▼                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                  Mac mini (實體伺服器)                       │ │
│  │                                                             │ │
│  │   joey-ai-agent (FastAPI)              port 8000            │ │
│  │      ├── LINE Webhook 處理                                  │ │
│  │      ├── Web API 端點                                       │ │
│  │      ├── Claude Code 任務執行                               │ │
│  │      └── Notion API 整合                                    │ │
│  │                                                             │ │
│  │   Claude Code CLI                                           │ │
│  │      └── 實際執行網站建立任務                                │ │
│  │                                                             │ │
│  │   資料儲存：Notion Database                                  │ │
│  │      ├── Inbox（待處理任務）                                 │ │
│  │      ├── Review（已處理任務）                                │ │
│  │      └── Memory（記憶/偏好）                                 │ │
│  │                                                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 部署流程詳解

### Web 前端部署流程

```
┌─────────────────┐      git push      ┌─────────────────┐      自動偵測      ┌─────────────────┐
│   Mac mini      │ ──────────────────→│    GitHub       │ ──────────────────→│    Render       │
│                 │                    │                 │                    │                 │
│ /Users/joeyserver                    │ joey-ai-frontend│                    │ Static Site     │
│ /web-frontend   │                    │ repo            │                    │ 自動部署        │
└─────────────────┘                    └─────────────────┘                    └─────────────────┘
                                                                                      │
                                                                                      ▼
                                                                              joey-ai-frontend
                                                                              .onrender.com
```

**步驟**：
1. 在 Mac mini 上編輯 `/Users/joeyserver/web-frontend` 的程式碼
2. `git add . && git commit && git push` 推送到 GitHub
3. Render 自動偵測 `joey-ai-frontend` repo 的變更
4. Render 自動重新建置並部署
5. 新版本上線到 https://joey-ai-frontend.onrender.com

### 後端部署流程

```
┌─────────────────┐      git push      ┌─────────────────┐
│   Mac mini      │ ──────────────────→│    GitHub       │
│                 │                    │                 │
│ /Users/joeyserver                    │ joey-ai-agent   │
│ /joey-ai-agent  │                    │ repo            │
└─────────────────┘                    └─────────────────┘
        │
        │ 手動重啟
        ▼
┌─────────────────┐
│  uvicorn 服務   │
│  port 8000      │
└─────────────────┘
        │
        │ Cloudflare Tunnel
        ▼
  ai-agent.89115053.xyz
```

**步驟**：
1. 在 Mac mini 上編輯 `/Users/joeyserver/joey-ai-agent` 的程式碼
2. `git add . && git commit && git push` 推送到 GitHub（備份用）
3. 重啟 uvicorn 服務：
   ```bash
   pkill -f 'uvicorn src.main:app'
   nohup python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 > /tmp/uvicorn.log 2>&1 &
   ```
4. 透過 Cloudflare Tunnel 對外提供服務

---

## 各 Repo 詳細說明

### 1. `joey-ai-agent` (主要後端)

| 項目 | 內容 |
|------|------|
| GitHub | https://github.com/JoeyVIP/joey-ai-agent |
| 位置 | Mac mini `/Users/joeyserver/joey-ai-agent` |
| 用途 | LINE Bot、Claude Code 執行、Notion 整合 |
| 運行 | `uvicorn src.main:app --host 0.0.0.0 --port 8000` |
| 對外 | https://ai-agent.89115053.xyz |

**重要檔案**：
- `src/api/line_webhook.py` - LINE Bot 邏輯
- `src/services/task_processor.py` - 任務處理流程
- `src/services/claude_code_service.py` - Claude Code 執行

### 2. `joey-ai-agent-local` (本地開發)

| 項目 | 內容 |
|------|------|
| GitHub | https://github.com/JoeyVIP/joey-ai-agent-local |
| 位置 | 本地 `/Users/JoeyLiao/Joey's AI Agent /joey-ai-agent` |
| 用途 | 在 JoeyLiao 電腦上開發和測試 |
| 部署 | 無（僅備份用）|

**注意**：這是獨立的 repo，commit history 與 `joey-ai-agent` 不同。

### 3. `joey-ai-frontend` (Web 前端)

| 項目 | 內容 |
|------|------|
| GitHub | https://github.com/JoeyVIP/joey-ai-frontend |
| 位置 | Mac mini `/Users/joeyserver/web-frontend` |
| 用途 | Web 介面、GitHub OAuth、專案進度監控 |
| 部署 | Render Static Site |
| 對外 | https://joey-ai-frontend.onrender.com |

---

## Render 服務狀態

| 服務 | 類型 | 狀態 | 說明 |
|------|------|------|------|
| joey-ai-frontend | Static Site | ✅ 使用中 | Web 前端，從 GitHub 自動部署 |
| joey-ai-agent-api | Web Service | ❌ 未使用 | 原本想部署後端，但 Claude Code 無法執行 |
| joey-ai-db | PostgreSQL | ❌ 未使用 | 原本為 Web 準備，現用 Notion 代替 |

### 為什麼 Render 後端沒在用？

**原因**：Claude Code CLI 需要完整的系統環境（SSH、Git、npm 等），Render 的容器環境無法滿足，所以後端必須留在 Mac mini。

**結論**：`joey-ai-agent-api` 和 `joey-ai-db` 可以刪除。

---

## 常用操作指令

### SSH 連線到 Mac mini
```bash
ssh macmini-remote
```

### 查看後端 log
```bash
ssh macmini-remote "tail -f /tmp/uvicorn.log"
```

### 重啟後端服務
```bash
ssh macmini-remote "cd /Users/joeyserver/joey-ai-agent && pkill -f 'uvicorn src.main:app'; sleep 2; nohup python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 > /tmp/uvicorn.log 2>&1 &"
```

### 更新後端程式碼
```bash
ssh macmini-remote "cd /Users/joeyserver/joey-ai-agent && git pull"
```

### 更新前端程式碼並部署
```bash
ssh macmini-remote "cd /Users/joeyserver/web-frontend && git add . && git commit -m 'Update' && git push"
# Render 會自動偵測並部署
```

### 檢查服務狀態
```bash
# 後端健康檢查
curl https://ai-agent.89115053.xyz/health

# 前端（檢查是否可訪問）
curl -I https://joey-ai-frontend.onrender.com
```

---

## 快速參考表

| 我想要... | 去哪裡 / 怎麼做 |
|-----------|----------------|
| 修改 LINE Bot 邏輯 | Mac mini: `/Users/joeyserver/joey-ai-agent/src/api/line_webhook.py` |
| 修改 Web 前端 | Mac mini: `/Users/joeyserver/web-frontend/` → git push → Render 自動部署 |
| 查看 Claude Code 執行 | Mac mini: `ps aux \| grep claude` |
| 新增授權使用者 | 修改 `line_webhook.py` 的 `AUTHORIZED_USERS` |
| 查看任務記錄 | Notion Review Database |

---

## 授權使用者管理

目前授權使用者定義在 `src/api/line_webhook.py`：

```python
ADMIN_USER_ID = settings.joey_line_user_id  # Joey（總管理員）

AUTHORIZED_USERS = {
    settings.joey_line_user_id: "Joey",           # 從 .env 讀取
    "U07923894d7eb396901da0796ee96d0c6": "Cindy", # 寫死在程式碼
}
```

**行為**：
- Joey 發訊息 → 正常處理
- Cindy 發訊息 → 通知 Joey + 正常處理
- 其他人 → 回覆「沒有權限」
